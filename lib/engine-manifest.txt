# The engine file set -- the SINGLE source of truth, consumed by:
#   gui/app/engine.py      (runtime materialization into the app data dir)
#   gui/rsyncronizer.spec  (build-time bundling)
#   cli/install-cli.sh     (no-GUI installs)
#   tests/run-tests.sh     (consistency check)
# One path per line; blanks and # comments ignored. The manifest ships itself
# so an installed engine home can in turn be an install source.
#
# A leading '?' marks an OPTIONAL entry: ship it if it is there, skip it if it
# is not. It exists for bin/rclone, which the desktop builds download at build
# time -- so it is present in an app bundle and absent from the repo and from
# the CLI tarball. Every consumer above understands the marker, which is what
# keeps this file the ONLY place the engine file set is written down.
setup.sh
status.sh
remote.sh
ssh-setup.sh
rsync-ignore.txt
VERSION
lib/common.sh
lib/cloud.sh
lib/rclone-version.txt
lib/trigger-macos.sh
lib/trigger-linux.sh
lib/wait-for-mount.sh
lib/support-qr.txt
lib/engine-manifest.txt
tests/cron-smoke-test.sh
cli/rsyncronizer
?bin/rclone
